# Data parameters
# flag of the parameters varying
keys : ['rate', 'neurons', 'time']
# Background firing rate in Hz
rate : [15, 30, 45, 60, 75]
# Length of data
t_stop : [3, 6, 9, 12, 15]
# Number of spiketrains
n : [100, 200, 300, 400, 500]
# SPADE parameters
# Length sliding window
winlen : 50
# Binsize for the analysis in ms
binsize : 1
